diff --git a/css/style.css b/css/style.css
index b392435..cc01e8d 100644
--- a/css/style.css
+++ b/css/style.css
@@ -178,11 +178,6 @@ ul {
   box-shadow: none;
 }
 
-/* Make logo white when navbar is transparent over dark backgrounds */
-.navbar:not(.scrolled) .logo img {
-  filter: brightness(0) invert(1);
-}
-
 .nav-container {
   display: flex;
   justify-content: space-between;
@@ -204,10 +199,14 @@ ul {
 }
 
 .navbar .logo img {
-  height: 65px;
-  /* Reduced from 105px for better spacing */
+  height: 75px;
+  /* Increased from 65px for better visibility */
   width: auto;
-  transition: var(--transition);
+  transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), filter 0.4s ease;
+}
+
+.navbar .logo img:hover {
+  transform: scale(1.08);
 }
 
 @media (max-width: 768px) {
@@ -216,7 +215,7 @@ ul {
   }
 
   .navbar .logo img {
-    height: 50px;
+    height: 60px;
   }
 }
 
@@ -230,7 +229,7 @@ ul {
   color: #ffffff;
   /* White when overlaying hero */
   position: relative;
-  font-size: 0.95rem;
+  font-size: 1.15rem;
   transition: color 0.4s ease;
 }
 
@@ -276,16 +275,18 @@ ul {
   position: absolute;
   top: 100%;
   left: 0;
-  background: var(--bg-light);
-  box-shadow: var(--card-shadow);
-  min-width: 220px;
+  background: rgba(255, 255, 255, 0.98);
+  backdrop-filter: blur(10px);
+  -webkit-backdrop-filter: blur(10px);
+  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
+  min-width: 240px;
   opacity: 0;
   visibility: hidden;
-  transform: translateY(10px);
-  transition: var(--transition);
-  border-top: 3px solid var(--accent-color);
-  border-radius: 0 0 8px 8px;
-  padding: 10px 0;
+  transform: translateY(15px);
+  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
+  border: 1px solid rgba(0, 0, 0, 0.05);
+  border-radius: 14px;
+  padding: 15px 0;
 }
 
 .dropdown:hover .dropdown-menu {
@@ -296,16 +297,34 @@ ul {
 
 .dropdown-item {
   display: block;
-  padding: 12px 20px;
+  padding: 12px 25px;
   color: var(--text-dark);
   font-weight: 500;
-  transition: var(--transition);
+  font-size: 0.95rem;
+  transition: all 0.3s ease;
+  position: relative;
+}
+
+.dropdown-item::before {
+  content: '';
+  position: absolute;
+  left: 0;
+  top: 0;
+  height: 100%;
+  width: 3px;
+  background-color: var(--accent-color);
+  transform: scaleY(0);
+  transition: transform 0.3s ease;
 }
 
 .dropdown-item:hover {
-  background-color: var(--bg-off-white);
+  background-color: rgba(7, 175, 223, 0.04);
   color: var(--accent-color);
-  padding-left: 25px;
+  padding-left: 32px;
+}
+
+.dropdown-item:hover::before {
+  transform: scaleY(1);
 }
 
 /* Mobile Nav */
@@ -339,6 +358,8 @@ ul {
     box-shadow: none;
     transform: none;
     border-top: none;
+    border: none;
+    background: transparent;
     display: none;
     padding-left: 20px;
   }
